(records-disposition-search-text-design)=
# Search Text Design Disposition

## Role

本页记录 `search_text` exploratory design draft 的最终处置。

## Trigger

触发源对象：

- `docs/search_text_design.md`

## Disposition Action

- superseded as live contract source
- retained as exploratory background only

## Successor

当前 successor / canonical host 为：

- `10_knowledge/70_interfaces/search_text_ux_contract.md`

## Judgment

当 `search_text` 的 accepted UX contract 已进入现行 knowledge family 后，
旧的 exploratory design draft 不应继续充当 live contract 或 implementation authority。

因此，本次处置采取：

- 将设计草稿降为历史背景材料；
- 将对外 contract authority 交给现行 interfaces page；
- 保留该草稿作为早期设计探索的可追溯来源，而不再让它参与当前裁判。

## Boundary

本页只记录该 draft 的命运；
不重写 `search_text` 的当前 interface contract 本体。
